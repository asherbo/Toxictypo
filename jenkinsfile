pipeline {
    agent any
    stages {
        stage('---Clone From GitHub---') {
            steps {
                git 'https://github.com/asherbo/Toxictypo.git'
                sh 'rm -rf .git'
            }
        }

    }
}
