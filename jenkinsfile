pipeline {
    agent any
    stages {
        stage('---Clone From GitHub---') 
        {
            steps {
                git 'https://github.com/asherbo/Toxictypo.git'
                sh 'rm -rf .git'
            }
        }
        stage("build")
        {
            steps
            {
                script
                {   
                    sh 'docker pull maven:3-alpine'
                    sh 'docker run -it -v docker container run -ti -v  /home/asher/Desktop/JENKINS-CI-CD-AWS-ToxicTypoProject-master:/app maven:3-alpine sh'
                }
            }
        }

    }
}
